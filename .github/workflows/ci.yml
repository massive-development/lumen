name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-go:
    name: Build & Vet Go Services
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - { name: memory,  dir: memory }
          - { name: tools,   dir: tools  }
          - { name: gpu,     dir: gpu    }
          - { name: cache,   dir: cache  }
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - uses: actions/setup-go@v6
        with:
          go-version-file: ${{ matrix.service.dir }}/go.mod
          cache-dependency-path: ${{ matrix.service.dir }}/go.sum

      - name: Build ${{ matrix.service.name }}
        working-directory: ${{ matrix.service.dir }}
        run: go build ./...

      - name: Vet ${{ matrix.service.name }}
        working-directory: ${{ matrix.service.dir }}
        run: go vet ./...

  build-proxy:
    name: Build & Vet Proxy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: false

      - name: Build proxy
        run: |
          go mod init bitnet/proxy
          go mod tidy
          go build -o /dev/null proxy.go
          go vet proxy.go

  lint-python:
    name: Lint Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Lint
        run: ruff check keygen.py gpu/worker.py

  docker-build:
    name: Docker Build Check
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: lumen-cpu
            dockerfile: Dockerfile
            context: .
          - name: lumen-proxy
            dockerfile: proxy.Dockerfile
            context: .
          - name: lumen-memory
            dockerfile: memory/Dockerfile
            context: memory
          - name: lumen-tools
            dockerfile: tools/Dockerfile
            context: tools
          - name: lumen-gpu
            dockerfile: gpu.Dockerfile
            context: .
          - name: lumen-cache
            dockerfile: cache/Dockerfile
            context: cache
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - uses: docker/setup-buildx-action@v4

      - name: Build ${{ matrix.name }}
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
